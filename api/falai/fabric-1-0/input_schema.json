{
  "$defs": {
    "ResolutionEnum": {
      "description": "Resolution options for video generation.",
      "enum": [
        "480p",
        "720p"
      ],
      "title": "ResolutionEnum",
      "type": "string"
    }
  },
  "properties": {
    "image": {
      "description": "Image to turn into a talking video. Supported formats: JPEG, PNG, WebP",
      "format": "file",
      "title": "Image",
      "type": "string"
    },
    "audio": {
      "description": "Audio file for the talking video. Supported formats: WAV, MP3",
      "format": "file",
      "title": "Audio",
      "type": "string"
    },
    "resolution": {
      "$ref": "#/$defs/ResolutionEnum",
      "default": "480p",
      "description": "Video resolution. Higher resolutions provide better quality but take longer to generate."
    }
  },
  "required": [
    "image",
    "audio"
  ],
  "title": "AppInput",
  "type": "object"
}