{
  "description": "Define your setup schema here.\n\nSetup parameters are provided once when the app instance starts.\nUse them for configuration that affects initialization, not per-request data.",
  "properties": {
    "model_variant": {
      "default": "base",
      "description": "Model variant to load (base, large, xl)",
      "title": "Model Variant",
      "type": "string"
    },
    "use_fp16": {
      "default": true,
      "description": "Use half precision for faster inference",
      "title": "Use Fp16",
      "type": "boolean"
    },
    "enable_cache": {
      "default": false,
      "description": "Enable result caching",
      "title": "Enable Cache",
      "type": "boolean"
    }
  },
  "title": "AppSetup",
  "type": "object"
}