pydantic >= 2.0.0 # required for inference.sh
inferencesh # required for inference.sh

# Google Vertex AI (for credentials)
google-genai>=1.0.0

# Image processing
Pillow>=10.0.0

# Async HTTP for Vertex AI REST API
aiohttp>=3.9.0
