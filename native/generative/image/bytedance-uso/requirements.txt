# Core inference.sh requirements
pydantic >= 2.0.0
inferencesh

# ML frameworks with specific versions for stability
# pinned: torch >= 2.11 pulls nvidia-nccl-cu13, which fails to load on the
# CUDA 12 grid nodes (undefined symbol: ncclCommResume)
torch==2.8.0
torchvision==0.23.0
torchaudio==2.8.0

# Acceleration libraries
accelerate>=1.1.1
# pinned: transformers 5.x drops slow tokenizers (T5 conversion then needs
# protobuf), and tokenizers 0.23 prereleases break RobertaProcessing(cls=...)
transformers==5.10.0
tokenizers==0.22.0

# Utility libraries
numpy>=1.23.5,<2
pillow
# pinned: deploy compiles with --prerelease allow, which otherwise pulls 0.9.0.dev0
einops==0.8.1

# USO specific dependencies
diffusers>=0.38.0
huggingface-hub
# required by HF_HUB_ENABLE_HF_TRANSFER=1 in inf.yml
hf_transfer
sentencepiece
safetensors>=0.4.5
scipy>=1.10.1
opencv-python
matplotlib

# CLIP for vision processing
git+https://github.com/openai/CLIP.git
