{
  "properties": {
    "video": {
      "description": "Input video file to interpolate",
      "format": "file",
      "title": "Video",
      "type": "string"
    },
    "target_fps": {
      "default": 30,
      "description": "Target output FPS.",
      "enum": [
        24,
        30,
        60,
        120
      ],
      "title": "Target Fps",
      "type": "integer"
    },
    "fast_mode": {
      "default": true,
      "description": "Enable fast mode. Might reduce quality.",
      "title": "Fast Mode",
      "type": "boolean"
    },
    "slow_motion": {
      "default": false,
      "description": "Enable slow-motion mode. Video will be played at half the interpolated frame rate.",
      "title": "Slow Motion",
      "type": "boolean"
    }
  },
  "required": [
    "video"
  ],
  "title": "AppInput",
  "type": "object"
}