{
  "properties": {
    "prompt": {
      "description": "Text prompt for video generation",
      "title": "Prompt",
      "type": "string"
    },
    "negative_prompt": {
      "default": "oversaturated, overexposed, static, blurry details, subtitles, stylized, artwork, painting, still image, overall gray, worst quality, low quality, JPEG artifacts, ugly, deformed, extra fingers, poorly drawn hands, poorly drawn face, malformed, disfigured, deformed limbs, fused fingers, static motionless frame, cluttered background, three legs, crowded background, walking backwards",
      "description": "Negative prompt to guide what to avoid in generation",
      "title": "Negative Prompt",
      "type": "string"
    },
    "resolution": {
      "default": "720p",
      "description": "Resolution preset",
      "enum": [
        "480p",
        "720p"
      ],
      "title": "Resolution",
      "type": "string"
    },
    "width": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Width of the generated video (auto-set based on resolution if not specified)",
      "title": "Width"
    },
    "height": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Height of the generated video (auto-set based on resolution if not specified)",
      "title": "Height"
    },
    "num_frames": {
      "default": 81,
      "description": "Number of frames to generate",
      "title": "Num Frames",
      "type": "integer"
    },
    "guidance_scale": {
      "default": 4.0,
      "description": "Primary classifier-free guidance scale",
      "title": "Guidance Scale",
      "type": "number"
    },
    "guidance_scale_2": {
      "default": 3.0,
      "description": "Secondary guidance scale",
      "title": "Guidance Scale 2",
      "type": "number"
    },
    "num_inference_steps": {
      "default": 40,
      "description": "Number of denoising steps",
      "title": "Num Inference Steps",
      "type": "integer"
    },
    "fps": {
      "default": 16,
      "description": "Frames per second for the output video",
      "title": "Fps",
      "type": "integer"
    },
    "seed": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Random seed for reproducibility",
      "title": "Seed"
    },
    "cache_threshold": {
      "default": 0,
      "description": "Cache threshold for transformer (0 to disable caching)",
      "title": "Cache Threshold",
      "type": "number"
    },
    "cache_threshold_2": {
      "default": 0,
      "description": "Cache threshold for transformer_2 (0 to disable caching)",
      "title": "Cache Threshold 2",
      "type": "number"
    },
    "video_output_quality": {
      "default": 5,
      "description": "Video output quality (1-9)",
      "maximum": 9,
      "minimum": 1,
      "title": "Video Output Quality",
      "type": "integer"
    }
  },
  "required": [
    "prompt"
  ],
  "title": "AppInput",
  "type": "object"
}