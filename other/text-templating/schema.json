{
  "input": {
    "properties": {
      "template": {
        "description": "The text template with placeholders like {1}, {2}, etc.",
        "title": "Template",
        "type": "string"
      },
      "strings": {
        "description": "List of strings to substitute into the template",
        "items": {
          "type": "string"
        },
        "title": "Strings",
        "type": "array"
      }
    },
    "required": [
      "template",
      "strings"
    ],
    "title": "AppInput",
    "type": "object"
  },
  "output": {
    "properties": {
      "result": {
        "description": "The processed template with strings substituted",
        "title": "Result",
        "type": "string"
      }
    },
    "required": [
      "result"
    ],
    "title": "AppOutput",
    "type": "object"
  }
}